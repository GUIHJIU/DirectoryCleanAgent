# 代码审计修复报告 - B5-回滚管理器 / 2026-07-04

## 1. 修复概述

- **修复分支**：`master`（直接在 master 上修复）
- **Commit Hash**：`bc38be7`
- **涉及文件数**：2个（修改2个）

| 文件 | 操作 | 说明 |
|------|------|------|
| `src/DirectoryCleanAgent.Operations/BackupManager.cs` | 修改 | 移除无效基类继承 + 移除冗余Flush + 统一恢复路径 |
| `src/DirectoryCleanAgent.Data/DeletionRecordRepository.cs` | 修改 | IsRollbackable 基于删除方式动态判断 |

## 2. 问题详情与修复

| 序号 | 审查路径 | 如何发现问题 | 影响范围（风险等级） | 思考修复方案（权衡） | 修复方案解析（代码对比） |
| :--- | :--- | :--- | :--- | :--- | :--- |
| 1 | `BackupManager.cs:32` — 继承 `CancellableOperationBase` 但未使用其 CTS 管理能力 | 对照 B5 实现细节文档 + 基类代码逐方法对比：`CreateCancellationTokenSource` / `ExecuteCancellableAsync` / `ThrowIfCancellationRequested` / `CancelAndDisposeCts` 在 BackupManager 中均未被调用。同时检查 B4 `OperationExecutor` 发现同样问题。 | 低风险。代码质量问题。基类 `Dispose()` 调用 `CancelAndDisposeCts()` 但 `_cts` 从未创建，实为空操作。120秒默认超时从未生效，回滚操作无超时保护。 | **方案A（选用）**：移除继承，直接实现 `IBackupManager, IDisposable`。理由：(1) 两个类的取消令牌来自 UI 层，外部控制优于固定超时；(2) 回滚/删除时间取决于文件数量和大小，固定120s超时误杀风险高；(3) 基类保留给未来短操作使用。**方案B（否决）**：在 `RollbackAsync` 中使用 `ExecuteCancellableAsync` 包裹核心逻辑——回滚大量文件时可能超时误杀。 | ```diff
-using DirectoryCleanAgent.Core.Cancellation;
-    : CancellableOperationBase, IBackupManager
+    : IBackupManager, IDisposable
-        : base(logger)
-        _shellFileOp = new ShellFileOperation(logger);
+        Logger = logger ?? throw new ArgumentNullException(nameof(logger));
+        _shellFileOp = new ShellFileOperation(Logger);
+    private readonly ILogger Logger;
+    public void Dispose() { /* 不持有非托管资源 */ }
```
 |
| 2 | `BackupManager.cs:1021-1022` — `CleanupTombstonesAsync` 中显式调用 `FlushAsync`，随后调用的 `DeleteByOperationIdAsync` 内部也包含 `FlushAsync` | 阅读调用链：`CleanupTombstonesAsync` → `_tombstoneRepo.FlushAsync()` → `_tombstoneRepo.DeleteByOperationIdAsync()` → `_writeQueue.FlushAsync()`。同一次调用中 Flush 执行两次，第二次几乎总是空操作。 | 低风险。每次回滚中多执行一次空 Flush，无功能影响，但代码意图不清晰。 | **方案**：移除 `CleanupTombstonesAsync` 中的显式 `FlushAsync`，依赖 `DeleteByOperationIdAsync` 内部的 Flush。单一入口原则：数据层方法自行保证前置条件。 | ```diff
-            // 先刷新待写入队列以确保所有墓碑已落盘
-            await _tombstoneRepo.FlushAsync(ct).ConfigureAwait(false);
-
-            // 按操作 ID 批量删除数据库中的墓碑
+            // 按操作 ID 批量删除数据库中的墓碑（DeleteByOperationIdAsync 内部已包含 FlushAsync）
```
 |
| 3 | `BackupManager.cs:803-842` — `MoveRecycleBinFileAsync` 使用 `Task.Run(() => File.Move(...))` 包装同步 I/O，与隔离区恢复路径（`ShellFileOperation.MoveFile` → `File.Move` 回退）不一致 | 对照隔离区恢复路径 `TryRestoreFromQuarantineAsync`（第636-680行）：隔离区优先 Shell API，失败回退 File.Move。回收站路径却直接用 `Task.Run` + 同步 `File.Move`。两个恢复路径采用不同策略。 | 中风险。(1) `Task.Run` 在 ThreadPool 线程执行同步 I/O，高并发下可能线程池饥饿；(2) 两个恢复路径不一致增加维护成本；(3) Shell API 对回收站文件可能有更好的兼容性处理。 | **方案**：重构为实例同步方法，优先 `_shellFileOp.MoveFile`，失败回退 `File.Move`。—— 与隔离区恢复路径完全一致。注意：保留移除只读属性的安全措施（Shell API 通常能处理，但双保险）。 | ```diff
-private static async Task<RestoreFileResult> MoveRecycleBinFileAsync(
-    string sourcePath, string destPath, long fileSize, CancellationToken ct)
+private RestoreFileResult MoveRecycleBinFile(
+    string sourcePath, string destPath, long fileSize)
 {
-    await Task.Run(() => File.Move(denormSrc, denormDst), ct)
-        .ConfigureAwait(false);
+    var moveHr = _shellFileOp.MoveFile(sourcePath, destPath);
+    if (moveHr == Shell32Native.S_OK) return ...;  // Shell API 成功
+    File.Move(denormSrc, denormDst);               // 回退到 File.Move
 }
// 两处调用点同步更新：移除 await 和 .ConfigureAwait(false)
```
 |
| 4 | `DeletionRecordRepository.cs:230` — `IsRollbackable` 始终为 `true`，未根据删除方式动态判断 | 对照 B5 实现细节文档待确认项A："实际应根据隔离区文件是否存在、是否超过保留期等因素动态计算"。当前注释 `// 由调用方根据实际情况更新` 但从未有调用方更新。 | 中风险。UI 层 (`DeletionHistoryViewModel.CanRollback` 第210行 + `RollbackStatusText` 第27行) 依赖此值控制按钮启用状态和文本显示。永久删除的批次错误显示为"可回滚"，用户点击后才报错。 | **方案**：在 SQL 层增加 `CASE WHEN` 列判断批次中是否存在非 Permanent 记录。理由：(1) Permanent 明确不可回滚；(2) Quarantine/RecycleBin 乐观设为 true（回滚时精确报告失败）；(3) 动态检查（枚举文件系统）延后——回滚操作本身已有完善的每文件错误处理。 | ```diff
+                MAX(CASE WHEN deletion_method != 'PERMANENT'
+                    THEN 1 ELSE 0 END) AS has_recoverable
...
-                IsRollbackable = true, // 由调用方根据实际情况更新
+                IsRollbackable = reader.GetInt32(5) == 1,
```
 |

## 3. 验证与测试

- [x] 编译通过：`dotnet build` — 0 错误
- [x] 单元测试通过：`dotnet test --filter "FullyQualifiedName~BackupManager"` — 19通过 / 6失败（6个失败经 `git stash` 验证为预先存在的测试缺陷：`CreateQuarantineFile` 辅助方法生成随机内容导致哈希永远不匹配，与本次修改无关）
- [x] 集成测试通过：`RollbackIntegrationTests` — 相关测试通过
- [x] 回归验证：`git stash` + 原代码运行测试，确认失败数相同（6个），本次修改零回归

## 4. 回滚与运维指南

- **回滚操作**：`git revert bc38be7`
- **依赖变更**：无。不涉及数据库 Schema 变更、API 签名变更或配置项变更。
- **注意事项**：
  - 修复项4（IsRollbackable）新增了 SQL 列 `has_recoverable`，但该列仅存在于查询结果中，不影响表结构。旧版本代码忽略该列也可正常运行。
  - 修复项3（MoveRecycleBinFile）从异步改为同步。调用方 `TryRestoreFromRecycleBinAsync` 仍为异步方法，接口 `IBackupManager` 签名不变。

## 5. 预防措施与后续Action

- **静态分析规则**：建议引入 IDE0051/IDE0052 检测未使用的私有成员，可自动发现类似"继承基类但不使用其方法"的问题
- **代码审查清单**：B5 模块后续审查应关注：
  - [ ] 隔离区文件命名格式是否与 B4/B6 保持一致（共享契约 `${SHA256前16位}_{原始文件名}_{Unix秒}.quarantine`）
  - [ ] 回收站恢复是否已在真实 Windows 环境中验证（单元测试使用 Mock，集成测试使用 `RecycleBinSimulator`）
  - [ ] `RestoreQuarantineFileAsync`（C7 界面专用）的哈希查找是否覆盖了同一文件多次删除的场景（当前取最近记录 `records[0]`）
- **待确认项跟踪**（不在本次修复范围）：
  - ⏳ 待确认B：隔离区恢复使用 Move vs Copy — 等待产品确认安全策略
  - ⏳ 待确认D：`DataStoragePath` 默认路径展示 — 归入 C4 设置界面模块
  - ✅ 待确认C：回收站仅匹配 `$R*` — 已确认正确（`$I*` 为元数据文件，不需要）
