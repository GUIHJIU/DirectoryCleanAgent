# 代码审计修复报告 - A1.5 / 2026-07-02

## 1. 修复概述

- **修复分支**：`master`
- **Commit Hash**：`1a6c45b`
- **涉及文件数**：15 个（Core 4 + Data 1 + Operations 2 + Decision 1 + Tests 7）
- **审查模块**：A1.5 — 核心服务契约（接口与 DTO）
- **审查依据**：`docs/任务分解.md` DoD + `docs/任务分解-补充说明.md` 第 3 节 + `docs/智能磁盘清理工具-总体设计文档-V3.7.md`

## 2. 问题详情与修复（核心部分）

| 序号 | 审查路径 | 如何发现问题 | 影响范围（风险等级） | 思考修复方案（权衡） | 修复方案解析（代码对比） |
| :--- | :--- | :--- | :--- | :--- | :--- |
| 1 | `src/DirectoryCleanAgent.Core/Enums/DeleteMethod.cs:6-13` | 对照设计文档 5.1 节 `deletion_method` 字段（`RECYCLE_BIN / QUARANTINE / PERMANENT`）与 `IOperationExecutor.ExecuteAsync` 文档注释（"回收站 / 隔离区 / 永久删除"），发现枚举仅有 `RecycleBin` 和 `Permanent`，缺少 `Quarantine` | **中风险**：B4 操作执行器无法通过枚举值区分隔离区模式；`DeletionRecord` 文档注释与枚举定义不一致；`OperationExecutor` 写 `"RECYCLEBIN"` 但 `BackupManager` 读 `"RECYCLE_BIN"`（附带发现不一致 bug） | **方案A**：保持枚举不变，DOC 注释改为仅含 RecycleBin/Permanent<br>**方案B（采用）**：添加 `Quarantine = 2`，与设计文档一致。<br>权衡：隔离区删除需通过 B4→B6 委托实现，枚举值用于标记 snaphost 中文件的删除方式，审计时区分三种路径。 | **Before**:<br>`enum DeleteMethod { RecycleBin = 0, Permanent = 1 }`<br>**After**:<br>`enum DeleteMethod { RecycleBin = 0, Permanent = 1, Quarantine = 2 }` |
| 2 | `src/DirectoryCleanAgent.Core/DTOs/DeletionRecord.cs:30` | 审查时发现 `DeletionRecord.DeletionMethod` 为 `string` 类型，而 `DeleteSnapshotEntry.FinalAction` 使用 `FinalAction` 强类型枚举，不一致。且 `OperationExecutor` 写入格式 `"RECYCLEBIN"`（`ToString().ToUpperInvariant()`）与 `BackupManager` 期望格式 `"RECYCLE_BIN"`（手动硬编码）不兼容——此为**附带发现的隐藏 bug**。 | **高风险**：字符串格式不一致导致回滚时无法正确识别删除方式，影响恢复路径选择。 | **方案A**：统一使用字符串常量（仅修复格式不一致）<br>**方案B（采用）**：改为 `DeleteMethod` 强类型枚举。<br>权衡：需更新 15 个文件（Data 层读写转换 + Operations 层模式匹配 + 测试用例），但换来编译期类型安全、消除格式不一致根源、`ParseDeleteMethod` 向后兼容历史数据（同时支持 "RECYCLEBIN" 和 "RECYCLE_BIN" 格式）。 | **DeletionRecord.cs**:<br>`- public required string DeletionMethod`<br>`+ public required DeleteMethod DeletionMethod`<br><br>**DeletionRecordRepository.cs**:<br>`ParseDeleteMethod` 优先 `Enum.TryParse` 不区分大小写，回退兼容历史带下划线格式<br><br>**BackupManager.cs**:<br>`- "QUARANTINE" =>`<br>`+ DeleteMethod.Quarantine =>` |
| 3 | `src/DirectoryCleanAgent.Core/DTOs/DecisionSnapshot.cs:61-69` + `DeleteSnapshotEntry.cs` | 对照设计文档 5.1 节 `decision_snapshot` JSON 结构：每个文件条目应含 `verdict` 字段记录规则裁决来源，wrapper 应含 `op`/`files`/`frozen_at`。当前 `ToCompactJson()` 仅序列化 `Entries` 裸数组，缺少 wrapper 结构；`DeleteSnapshotEntry` 无 `RuleVerdict` 字段。 | **低风险**：审计溯源时不完整——能知道文件"被删除"但无法知道"因何种规则被删除"。功能不受影响（删除操作本身不依赖此字段）。 | **方案A**：仅添加 `RuleVerdict` 字段（最小改动）<br>**方案B（采用）**：添加 `RuleVerdict` + wrapper 结构。<br>权衡：wrapper 中 `op` 和 `frozen_at` 在 `DeletionRecord` 中已有独立字段，属于冗余信息，但设计文档显式要求此格式，遵循规范。使用内部私有类 `SnapshotJsonWrapper` 仅用于序列化，不暴露为公共 API。 | **DeleteSnapshotEntry.cs**:<br>`+ public required RuleVerdict RuleVerdict`<br>`+ [JsonPropertyName("verdict")]`<br><br>**DecisionSnapshot.cs**:<br>`ToCompactJson()` 输出格式从 `[...]` 变为 `{"op":"...","files":[...],"frozen_at":"..."}` |

## 3. 验证与测试

- [x] **编译通过**：`dotnet build` 0 错误（94 个预先存在的 xUnit 分析器警告）
- [x] **契约接口测试**：`InterfaceContractTests` — 41/41 通过
- [x] **操作执行器测试**：`OperationExecutorTests` — 全部通过
- [x] **决策引擎测试**：`DecisionEngineTests` — 52/53 通过（1 个预先存在的进度上报测试失败，与本次更改无关）
- [x] **回归验证**：7 个预先存在的测试失败（BackupManager × 6 + DecisionEngine.Progress × 1）经 `git stash` 对比确认均为修复前既存问题
- [x] **集成测试**：编译通过（集成测试需要 Everything 运行环境，未在本次执行）

## 4. 回滚与运维指南

- **回滚操作**：`git revert 1a6c45b`
- **依赖变更**：无
- **数据库影响**：
  - `DeletionRecord.deletion_method` SQLite TEXT 列：新写入使用 `DeleteMethod.ToString().ToUpperInvariant()` 格式（如 `"RECYCLEBIN"`、`"QUARANTINE"`）
  - `ParseDeleteMethod` 向后兼容旧格式（`"RECYCLE_BIN"`、`"QUARANTINE"`），历史数据不受影响
  - `DecisionSnapshotJson` JSON 结构变更：新增 `op`/`files`/`frozen_at` wrapper，每个 file entry 新增 `verdict` 字段
- **API 兼容性**：`DeleteMethod` 枚举新增 `Quarantine = 2`，已编译的二进制兼容

## 5. 预防措施与后续 Action

- **静态扫描规则**：建议后续引入 Roslyn Analyzer 规则，强制要求 `enum` 成员变更时同步更新所有 `switch` 表达式（如 `CS8509` 非穷尽 switch 警告）
- **附带发现**：`OperationExecutor` 写入的 `DeletionMethod` 格式（`ToString().ToUpperInvariant()` = `"RECYCLEBIN"`）与 `BackupManager` 期望格式（硬编码 `"RECYCLE_BIN"`）不一致——本次修复通过统一使用强类型枚举消除了此问题，但建议后续审计检查其他模块中是否存在类似的字符串格式不一致
- **相关文档更新**：
  - `CLAUDE.md` 中 `DeleteSnapshotEntry` 定义建议补充 `rule_verdict` 字段
  - `docs/任务分解.md` A1.5 DoD 建议补充 `IQuarantineManager` 等后期发现额外所需的接口
- **后续任务**：问题 2 附带发现的 BackupManager 字符串格式不一致问题，建议在 B5（回滚管理器）实现阶段统一审查
