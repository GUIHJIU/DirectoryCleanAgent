# 代码审计修复报告 — B1 Everything 流式适配器

**审计日期**：2026-07-04
**设计基线**：V3.7
**审计范围**：`src/DirectoryCleanAgent.Everything/`、`src/DirectoryCleanAgent.Core/Interfaces/ITombstoneCache.cs`、`src/DirectoryCleanAgent.Data/TombstoneCache.cs`

---

## 1. 修复概述

- **修复分支**：`master`
- **Commit Hash**：`5b7a9f2e182ca180e83f1a9225c9bedef8efb8bb`
- **涉及文件数**：7 个（源码 5 个 + 测试 2 个）

---

## 2. 问题详情与修复（核心部分）

| 序号 | 审查路径 | 如何发现问题 | 影响范围（风险等级） | 思考修复方案（权衡） | 修复方案解析（代码对比） |
| :--- | :--- | :--- | :--- | :--- | :--- |
| 1 | `EverythingFileListProvider.cs:344-346` — 指纹键构造条件 | 对比实现文档 4.3 节墓碑过滤流程，发现指纹键仅依赖全局 `frnAvailable` 标记而非实际 FRN 键构造结果 | **高风险**：FRN 全局可用但单文件 FRN=0（非 NTFS 卷如 FAT32/exFAT），`frnKey=null` 且 `fingerprintKey=null`，文件永远无法被墓碑过滤 | **方案 A（推荐）**：改为 `frnKey is null` 驱动降级，覆盖全局不可用/rawFrn=0/volumeGuid=null 三种情况。**方案 B**：扩展条件显式判断（等价但冗余）。选择 A，逻辑自洽——"FRN 键构造成功→FRN 匹配；失败→指纹降级" | **修复前**：`string? fingerprintKey = !frnAvailable ? $"{sizeBytes}:{lastWriteTime:O}" : null;` **修复后**：`string? fingerprintKey = (frnKey is null) ? $"{sizeBytes}:{lastWriteTime:O}" : null;` |
| 2 | `ITombstoneCache.cs` → `TombstoneCache.cs:81-96` — 指纹匹配缺少路径/大小校验 | 对照 `LocalTombstone` DTO 注释"额外校验 file_path 和 original_size 一致性"，发现 `IsTombstoned` 仅做 `ContainsKey` 检查 | **中风险**：两个不同文件在同一 100ns tick 内相同大小和修改时间时（文件复制保留时间戳场景），指纹碰撞导致误过滤。概率极低但不为零 | **方案 A（推荐）**：扩展接口添加可选 filePath/fileSize 参数 + TryGetValue 替换 ContainsKey + 路径/大小一致性校验。可选参数向后兼容。**方案 B**：仅修正 DTO 注释（放弃校验）。选择 A，热路径增加 ~5-10ns 开销可接受 | **修复前**：`if (fingerprintKey is not null && _fingerprintCache.ContainsKey(fingerprintKey)) return true;` **修复后**：`if (fingerprintKey is not null && _fingerprintCache.TryGetValue(fingerprintKey, out var t)) { if (filePath != null && fileSize.HasValue) { if (t.OriginalSize != fileSize.Value \|\| !string.Equals(t.FilePath, filePath, OrdinalIgnoreCase)) return false; } return true; }` |
| 3 | `EverythingFileListProvider.cs:355` — EverythingSortKey 填充为循环索引 | 审查 `FileItem.EverythingSortKey` 字段实际用途，grep 确认下游 UI 层透传后无消费者 | **低风险**：功能字段无实际作用，循环索引在 Everything 结果集变化后失效 | **方案 A（推荐）**：改为 `normalizedPath`，文件路径是天然唯一标识。**方案 B**：改为 FRN 键（非 NTFS 退化）。**方案 C**：移除字段。选择 A，语义正确且可用于未来重查 | **修复前**：`string sortKey = i.ToString();` **修复后**：`string sortKey = normalizedPath;` |
| 4 | `EverythingFileListProvider.cs:237-241` — MaxResults 未下推 Everything SDK | 审查枚举流程发现 MaxResults 仅在托管侧截断，`Everything_SetMax` P/Invoke 已定义但未在枚举中使用 | **低风险**：功能正确（结果数正确），性能未最优——Everything 仍处理全部结果。SDK 2.0 全局状态约束下 save/restore 模式需要接口扩展 | **当前决策**：暂不修复。理由：(1) SDK 2.0 全局状态需 save/restore；(2) 流式架构已保证内存 <200MB；(3) 待 D3 性能测试评估。添加 TODO 注释记录决策 | **修复后**：添加注释说明决策原因（SDK 2.0 全局持久状态 + save/restore 需要 IEverythingSdk 接口扩展），标注 `TODO(D3)` |
| 5 | `EverythingFileListProvider.cs:118-132` — IsIndexingAsync 未获取 SDK 锁 | 审查锁使用一致性时发现，对比枚举流程的锁保护模式 | **低风险**：`Everything_IsDBLoaded()` 是 SDK 内部只读 bool 查询，不修改全局搜索状态，线程安全 | **方案 A（推荐）**：确认安全 + 添加注释。**方案 B**：加锁（引入不必要的阻塞——索引状态查询被枚举阻塞）。选择 A | **修复后**：在 `<remarks>` 中添加"`IsDBLoaded` 是只读操作，不修改 SDK 全局状态，无需获取 `_sdkLock`" |
| 6 | `docs/任务实现细节/B1-Everything流式适配器.md` 第 4 节 — 锁范围描述与代码不一致 | 对照审查时发现文档写"锁外产出"但代码将 `yield return` 放在 `try/finally` 锁内 | **低风险**：文档与代码不一致，可能导致后续维护者误解 | 代码决策正确——SDK 2.0 全局状态下锁必须覆盖结果访问周期防 AccessViolationException。C# 支持 `try/finally` + `yield return`（仅禁止 `try/catch`），初始设计误判为编译错误。更新文档反映最终决策 | **修复后**：文档标题改为"锁内查询 + 锁内产出（已修订）"，新增修订原因说明和 C# yield return + try/finally 合法性注释 |

---

## 3. 验证与测试

- [x] **编译通过**：`dotnet build` — 0 错误，0 警告（新增代码部分）
- [x] **单元测试通过**：`dotnet test --filter "FullyQualifiedName~EverythingFileListProvider|FullyQualifiedName~TombstoneCache|FullyQualifiedName~EverythingSortMapper"` — **53 通过，0 失败，0 跳过**
- [x] **新增测试用例**：
  - `EnumerateFilesAsync_FrnAvailable_ButRawFrnZero_FallsBackToFingerprint` — 验证 FRN 可用但 rawFrn=0 时指纹降级
  - `EnumerateFilesAsync_EverythingSortKey_IsNormalizedPath` — 验证 SortKey 现在是标准化路径
  - `IsTombstoned_FingerprintMatch_ButPathMismatch_ReturnsFalse` — 验证指纹碰撞拒绝
  - `IsTombstoned_FingerprintMatch_PathAndSizeMatch_ReturnsTrue` — 验证路径+大小一致确认命中
  - `IsTombstoned_FingerprintMatch_NoVerificationParams_ReturnsTrue` — 验证向后兼容性
- [x] **接口向后兼容**：新增参数均为可选（默认值 `null`），不影响 `ITombstoneCache` 的其他实现和调用方

---

## 4. 回滚与运维指南

- **回滚操作**：`git revert 5b7a9f2e182ca180e83f1a9225c9bedef8efb8bb`
- **依赖变更**：无
- **数据库 Schema 变更**：无
- **接口破坏性变更**：无（`ITombstoneCache.IsTombstoned` 新增参数均为可选，编译兼容）

---

## 5. 预防措施与后续 Action

| 措施 | 说明 |
|------|------|
| 文档同步 | B1 实现文档已更新至与代码一致的锁范围描述 |
| 待 D3 性能测试确认 | MaxResults 下推优化（需先扩展 `IEverythingSdk` 接口添加 `SetMax`/`GetMax`） |
| 待确认项 B | 轮询间隔 3s 是否可接受——`ChangePollIntervalMs` 常量可调，等待 UX 反馈 |
| 待确认项 A | `FromFileTimeUtc()` 正确性——代码已正确使用，等待 Everything ≥1.4.1 + Windows 11 环境验证 |
| `FileListItem.EverythingSortKey` 注释 | 已修正为"文件标识键（文件路径，可用于后续 Everything 精确重查）" |

---

**审查结论**：B1 模块核心实现质量良好。24 项 DoD 中 24 项符合。6 项偏差已全部修复，其中 1 项高风险（FRN 降级 bug）、1 项中风险（指纹校验缺失）、4 项低风险（文档/注释/优化）。所有单元测试通过。
