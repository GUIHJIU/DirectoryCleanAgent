# 代码审计修复报告 - A2 / 2026-07-02

## 1. 修复概述

- **修复分支**：`master`（直接在主干修复）
- **Commit Hash**：`529c73e`
- **涉及文件数**：2 个
- **审查模块**：A2 — Everything SDK 集成与依赖检测
- **审查基线**：`docs/任务分解.md` A2 DoD + `docs/任务分解-补充说明.md` §4.1

## 2. 问题详情与修复（核心部分）

| 序号 | 审查路径 | 如何发现问题 | 影响范围（风险等级） | 思考修复方案（权衡） | 修复方案解析（代码对比） |
| :--- | :--- | :--- | :--- | :--- | :--- |
| 1 | `EverythingDetectionResult.cs:57` — `VersionOk` XML 注释 | 逐字段对照 DoD 时发现注释写 `≥ 1.5.0`，但 `EverythingDependencyDetector.cs:27-29` 定义的 `RequiredMajor/Minor/Revision` 为 `1.4.1`，注释与实际逻辑不一致 | **文档/注释级别（低）** — 不影响运行时行为，但可能误导后续开发者错误提升最低版本要求 | **方案 A**：注释改为 1.4.1，与代码常量一致。**方案 B**：代码常量改为 1.5.0 并测试。选择方案 A — 设计文档 V3.7 明确要求 Everything ≥ 1.4.1（FRN 稳定支持的起始版本），1.5.0 会无故排斥 1.4.1 用户。无测试负担。 | **修改前**：`/// <summary>版本号是否满足最低要求（≥ 1.5.0）</summary>` **修改后**：`/// <summary>版本号是否满足最低要求（≥ 1.4.1）</summary>` |
| 2 | `EverythingDependencyDetector.cs:183-198` — `RawVersion` 字段从未被填充 | 审查 DoD "Everything_GetVersion" 时发现 P/Invoke 已定义（`EverythingNative.cs:80`）但检测器从未调用，`RawVersion` 始终为 `0`（SDK 2.0 默认值）。独立 getter（`GetMajorVersion()` 等）已获取各字段，但原始编码版本号对调试有帮助 | **功能缺失（低）** — `RawVersion` 字段在 `EverythingDetectionResult` 中已定义但闲置，日志中缺少原始版本号不利于 SDK 版本问题排查 | SDK 2.0 的 `Everything_GetVersion()` 返回 DWORD 编码版本号（Bits 31-24=Major, 23-16=Minor, 15-8=Revision, 7-0=Build），独立 getter 虽然更可靠，但原始值对调试 SDK DLL 版本不匹配问题有参考价值。在成功返回时补充一行调用即可，零风险。 | **修改前**：`RawVersion = 0`（隐式默认）**修改后**：`RawVersion = EverythingNative.Everything_GetVersion()` |
| 3 | `EverythingDependencyDetector.cs:98` — 未探测 SDK 3.0 能力，`SdkVersion` 硬编码为 `2` | `Everything3Native.cs` 已完整定义 260+ 行 SDK 3.0 P/Invoke（命名管道架构），`EverythingDetectionResult` 已预留 `SdkVersion`/`ClientHandle` 字段，但检测器从未尝试 SDK 3.0 连接，代码能力未激活 | **功能缺失（中）** — 若用户安装 Everything 1.5+（SDK 3.0），检测走 SDK 2.0 路径虽然向后兼容（全局状态 API 仍可用），但无法利用命名管道的健壮性优势。B1 流式适配器等后续模块也无从知晓 SDK 3.0 可用性 | **方案 A**（选用）：在步骤 1（进程存活）之后插入轻量 `Everything3_ConnectW(null)` 试探，失败回退 SDK 2.0。耗时 <1ms，不改变 5 步主流程，仅记录能力标记供后续模块使用。**方案 B**：不做探测，将 SDK 3.0 支持推到 B1 阶段。选择方案 A — A2 的职责就是依赖检测，探测 SDK 版本是其自然延伸，且代码已就绪（`Everything3Native` 已完整定义），投入产出比极高。 | **修改前**：`SdkVersion = 2`（硬编码）**修改后**：步骤 1 之后插入 `Everything3_ConnectW(null)` 试探 → 成功则 `SdkVersion=3` 并保存 `ClientHandle` → `EntryPointNotFoundException`/其他异常回退 `SdkVersion=2` |

## 3. 验证与测试

- [x] **编译通过**：`dotnet build src/DirectoryCleanAgent.Everything/` — 0 错误，11 警告（均为已有的 NuGet 源不可达 + Windows API 平台警告）
- [x] **单元/集成测试通过**：`dotnet test --filter "FullyQualifiedName~Everything"` — 55/57 通过
  - 2 个失败为 B1 模块已有集成测试（`EverythingScanIntegrationTests`），与本次修改无关
  - A2 全部检测相关测试（IT-DETECT-01 ~ IT-DETECT-09 + 单元测试）均通过
- [x] **代码审查**：修改仅 2 文件 26 行新增/3 行删除，范围可控
- [x] **回归验证**：未修改任何公共 API 签名，不影响已有调用方

## 4. 回滚与运维指南

- **回滚操作**：`git revert 529c73e`
- **依赖变更**：无
- **配置变更**：无
- **数据库 Schema 变更**：无
- **注意事项**：SDK 3.0 探测在 `EntryPointNotFoundException` 时自动回退 SDK 2.0，不影响 Everything 1.4.x 用户。若探测过程中出现意外异常（如 Everything 1.5 的命名管道权限问题），同样回退 SDK 2.0 并记录 Warning 日志，不会阻断启动流程。

## 5. 预防措施与后续 Action

- **静态扫描规则**：建议后续引入 XML 注释与代码常量的交叉校验规则，避免注释版本号与实际常量不一致
- **相关文档更新**：本次无需更新设计文档（逻辑行为不变）
- **后续模块提示**：B1（Everything 流式适配器）可通过 `EverythingDetectionResult.SdkVersion` 字段选择 SDK API 版本：
  - `SdkVersion == 3`：优先使用 `Everything3Native`（命名管道，更健壮）
  - `SdkVersion == 2`：使用 `EverythingNative`（全局状态，兼容 1.4.x）
