# 代码审计修复报告 - C8-进度与取消UI集成 / 2026-07-09

## 1. 修复概述
- **修复分支**：`master`
- **Commit Hash**：`0158dc98df7f214e0f848db5afb727308cf02457`
- **涉及文件数**：2 个（`MainWindow.xaml`、`MainViewModel.cs`）
- **发现问题总计**：⚠️偏差 2 项，❓待确认 4 项
- **已修复**：偏差 2 项 + 待确认项 2 项
- **延后处理**：待确认项 2 项（❓C: IsIndeterminate 视觉样式、❓A: 默认卷假设已缓解无需修改）

## 2. 问题详情与修复（核心部分）

| 序号 | 审查路径 | 如何发现问题 | 影响范围（风险等级） | 思考修复方案（权衡） | 修复方案解析（代码对比） |
| :--- | :--- | :--- | :--- | :--- | :--- |
| 1 | `MainViewModel.cs:213` → `StatusBarInfo.cs:42` → `MainWindow.xaml:239` | 审查进度回调代码时发现 `StatusInfo.UpdateStatusText(p)` 写入 `StatusBarInfo.OperationStatusText`，但 XAML 绑定 `{Binding OperationStatusText}` 解析为 `MainViewModel.OperationStatusText`，两者为独立字段，数据流断链 | **高** — 操作进行中所有阶段描述文本（"正在扫描..."、"正在删除..."等）完全不显示，只有点击取消时的"正在取消..."能短暂显示 | **方案A（采纳）**: 修改 XAML 绑定为 `StatusInfo.OperationStatusText`，舍弃 MainViewModel 冗余属性。**权衡**: 需同步修改 ExecuteCancel 写入目标，但消除双属性维护成本。**方案B（否决）**: 在 5 个进度回调中各自追加 `OperationStatusText = StatusInfo.OperationStatusText`。**否决原因**: 重复代码多，后续新增回调易遗漏 | **XAML 修改**: `{Binding OperationStatusText}` → `{Binding StatusInfo.OperationStatusText}`。**ViewModel 修改**: 删除 `_operationStatusText` 字段和 `OperationStatusText` 属性；`ExecuteCancel` 中 `OperationStatusText = "正在取消..."` → `StatusInfo.OperationStatusText = "正在取消..."; OnPropertyChanged(nameof(StatusInfo))` |
| 2 | `MainViewModel.cs:466,616,980` | 偏差1修复后发现: `ExecuteCancel` 设置 MainViewModel 属性为"正在取消..."，但 `finally` 块中 `StatusInfo.ClearStatusText()` 只清除 StatusBarInfo 的属性 | **中** — 取消操作后状态栏残留"正在取消..."文本（偏差1修复后自动解决，因为改为统一写入 StatusInfo 路径） | 无需独立修复 — 偏差1的统一路径方案自动解决。`finally` 块中的 `ClearStatusText()` → `OnPropertyChanged(nameof(StatusInfo))` 链路已覆盖 | **无独立代码变更** — 与序号1合并修复。通知链路验证: `ClearStatusText()` 设置 `StatusInfo.OperationStatusText = ""` → `OnPropertyChanged(nameof(StatusInfo))` → WPF 重新绑定 `StatusInfo.OperationStatusText` → 清空 |
| 3 | `MainViewModel.cs:385-389` 容量检查弹窗 | 对照文档待确认项B: 弹窗文案"超出部分将直接永久删除"但代码 `ExecuteAsync(..., DeleteMethod.RecycleBin, ...)` 不切换删除方式 | **低** — 用户误以为文件会被永久删除，实际行为是删除失败进入 manual_review。不影响数据安全 | **方案A（采纳）**: 修正文案以准确描述行为。**权衡**: 修改文案为准确描述，不引入新的删除逻辑。**方案B（否决）**: 根据容量动态切换 `DeleteMethod.Permanent`。**否决原因**: 超出C8边界范围（文档明确"不做"），引入新的删除路径需要完整的安全审计 | **文案修改**: `"超出部分将直接永久删除。是否继续？"` → `"回收站空间不足，超出容量的文件将删除失败并标记为需人工处理。是否继续？"` |
| 4 | `MainViewModel.cs:1305-1328` `CollectCandidatesAsync` 进度报告 | 对照文档待确认项D: 每 100 个文件一次 `Dispatcher.Invoke`，2M 文件规模约 20,000 次 UI 线程调用 | **低** — 正常使用（<10万文件）约 1,000 次调用完全可接受。仅在极端场景（2M文件+高速SSD）可能造成 UI 轻微繁忙 | 添加 `_lastProgressUpdate` 字段和 200ms 最小间隔节流。**权衡**: 增加一个字段和两次 DateTime 比较，换取消极端场景下的 UI 稳定性。阈值 200ms 基于人类视觉感知（低于 5fps 刷新已足够流畅） | **新增字段**: `private DateTime _lastProgressUpdate = DateTime.MinValue;`。**循环内**: `if ((DateTime.Now - _lastProgressUpdate).TotalMilliseconds >= 200) { _lastProgressUpdate = now; Dispatcher.Invoke(...); }` |

## 3. 验证与测试

- [x] **编译通过**: `dotnet build` — 0 错误，18 警告（均为 NuGet 源连接问题，非代码问题）
- [x] **单元测试通过**: `dotnet test --filter "FullyQualifiedName~MainViewModelTests"` — 10/10 通过，0 失败，0 跳过
- [x] **通知链路验证**: 每条场景路径手动追踪，确认 `StatusInfo.OperationStatusText` 变更 → `OnPropertyChanged(nameof(StatusInfo))` → WPF 重新评估绑定 → UI 更新
- [x] **字段引用审计**: Grep 确认 `OperationStatusText` 仅剩 4 处引用（`MainWindow.xaml:239` 绑定、`StatusBarInfo.cs:24,42,62`），无遗漏引用

## 4. 回滚与运维指南

- **回滚操作**: `git revert 0158dc98df7f214e0f848db5afb727308cf02457`
- **依赖变更**: 无
- **配置变更**: 无
- **向后兼容**: 是。仅修改 UI 绑定路径和文案，不改变任何接口契约或数据格式

## 5. 预防措施与后续Action

- **是否引入新的静态扫描规则**: 建议添加 — 当 ViewModel 中存在与 Model 同名的属性时（如 `OperationStatusText` 同时存在于 `MainViewModel` 和 `StatusBarInfo`），应触发警告检查绑定路径是否正确
- **相关文档是否已更新**: 是，本报告即为更新文档
- **延后事项**:
  - ❓C `IsIndeterminate` 视觉样式: WPF 内置 Marquee 动画在当前 8px 高度进度条上可用，V3.8 统一处理主题定制
  - ❓A 默认卷 `C:` 假设: `JsonConfigService` 已通过 `Path.GetPathRoot(Environment.SystemDirectory)` 动态获取系统盘，双重保证 `IncludedVolumes` ≥ 1，风险已缓解无需修改
