# 代码审计修复报告 - B3-决策引擎 / 2026-07-04

## 1. 修复概述

- **修复分支**：`master`（直接在 master 上修复）
- **Commit Hash**：`1201eda3ddd949f84937ffc4580159fdc1ae2f6a`
- **涉及文件数**：5个（新增1个，修改4个）

| 文件 | 操作 | 说明 |
|------|------|------|
| `src/DirectoryCleanAgent.Core/Interfaces/IDecisionEngine.cs` | 修改 | 接口注释修正 |
| `src/DirectoryCleanAgent.Decision/DecisionEngine.cs` | 修改 | 异常分流 + AI标签常量化 + 并发度配置化 |
| `src/DirectoryCleanAgent.Decision/GlobalUsings.cs` | 修改 | 添加 Core.Constants 全局引用 |
| `src/DirectoryCleanAgent.Core/Config/UserConfig.cs` | 修改 | 新增 HashConcurrency 配置项 |
| `src/DirectoryCleanAgent.Core/Constants/AiLabelConstants.cs` | **新增** | AI 标签契约常量类 |

## 2. 问题详情与修复

| 序号 | 审查路径 | 如何发现问题 | 影响范围（风险等级） | 思考修复方案（权衡） | 修复方案解析（代码对比） |
| :--- | :--- | :--- | :--- | :--- | :--- |
| 1 | `IDecisionEngine.cs:25` — 接口注释描述 "使用 SemaphoreSlim(ProcessorCount) 控制并发" | 对照实现细节文档第一章：SemaphoreSlim 方案在架构设计阶段被明确否决（信号量泄漏风险），实际实现使用 `Parallel.ForEachAsync`。注释与实现不一致。 | 低风险。仅注释错误，不影响功能。但会误导后续维护者。 | **方案**：直接修正注释为实际并发策略。无任何代码逻辑变更。 | ```diff
- /// 3. 使用 SemaphoreSlim(ProcessorCount) 控制并发，逐文件计算 SHA-256 哈希
+ /// 3. 使用 Parallel.ForEachAsync(ProcessorCount) 控制并发，逐文件计算 SHA-256 哈希
```
|
| 2 | `IDecisionEngine.cs:32-33` — 进度注释 "每完成一个文件的哈希计算即调用一次" | 对照 `DecisionEngine.cs:106`：阶段 1 中 Protected 文件立即上报 `progress.Report(completedCount)`，从不进入哈希阶段。UI 层 (`MainViewModel.cs:399-414`) 已适配此行为。 | 低风险。注释与行为不一致，但调用方已正确适配。 | **方案**：精确化注释描述。不改变接口签名，保持 `IProgress<int>`（待确认项C已论证：UI 层完成适配器转换，`OperationProgress` DTO 已存在但无需在 B3 层暴露）。 | ```diff
- /// 每完成一个文件的哈希计算即调用一次 Report(completedCount)。
+ /// 每完成一个文件的仲裁即调用一次 Report(completedCount)（含 Protected 文件立即上报和哈希计算完成后上报）。
```
|
| 3 | `DecisionEngine.cs:161-177` — `UnauthorizedAccessException` 被标记为 "文件被其他进程占用" | 分析 Windows .NET FileStream 异常语义：<br>• `UnauthorizedAccessException` = ACL 拒绝读取（`0x80070005`），文件可能仍可删除<br>• `IOException + 0x80070020` = `ERROR_SHARING_VIOLATION`，文件被独占锁定<br>原代码将两者混淆，导致权限不足被误诊为文件锁定，而真正的文件锁定落入通用 Exception 处理。 | **中风险**。异常分类错误影响诊断准确性和 SkippedEntry 的可信度。 | **方案**：拆分为三个 catch 分支：<br>1. `UnauthorizedAccessException` → "权限不足，无法读取文件"<br>2. `IOException when (HResult & 0xFFFF) == 0x0020` → "文件被其他进程占用"<br>3. 通用 `Exception` → 保留原有 switch 逻辑<br><br>拒绝的方案：将 UnauthorizedAccessException 的文件纳入快照（无哈希）→ 增加复杂度且回滚验证不可靠。 | ```diff
- catch (UnauthorizedAccessException)
- { // 文件被其他进程独占锁定
-     Reason = "文件被其他进程占用，无法访问",
-     ExceptionType = nameof(UnauthorizedAccessException)
+ catch (UnauthorizedAccessException)
+ { // 权限不足（ACL 拒绝读取 或 路径为目录）
+     Reason = "权限不足，无法读取文件",
+     ExceptionType = nameof(UnauthorizedAccessException)
+ }
+ catch (IOException ioEx) when ((ioEx.HResult & 0xFFFF) == 0x0020)
+ { // ERROR_SHARING_VIOLATION → 文件被独占锁定
+     Reason = "文件被其他进程占用，无法访问",
+     ExceptionType = nameof(IOException)
```
|
| 4 | `DecisionEngine.cs:135` — `MaxDegreeOfParallelism = Environment.ProcessorCount` 硬编码 | 实现细节文档"待确认项A"：低配 HDD 设备（4核）可能磁盘 I/O 饱和。当前无 HDD/SSD 检测机制。 | 低风险。目标用户（开发者+SSD）通常不受影响，但面向普通用户时需要灵活配置。 | **方案**：引入 `UserConfig.HashConcurrency` 配置项（默认 0 = 自动）。值 >0 时取 `Math.Min(值, ProcessorCount)`。不引入 HDD 检测（Win32 API 调用复杂且跨版本不稳定）。 | ```diff
+ // UserConfig.cs 新增属性
+ public int HashConcurrency { get; set; } = 0;
+
+ // DecisionEngine.cs 使用配置
+ var hashConcurrency = config.HashConcurrency > 0
+     ? Math.Min(config.HashConcurrency, Environment.ProcessorCount)
+     : Environment.ProcessorCount;
+ MaxDegreeOfParallelism = hashConcurrency,
```
|
| 5 | `DecisionEngine.cs:336,340,348,376` — AI 标签 `"safe"`/`"unknown"`/`"risky"` 硬编码字符串 | 实现细节文档"待确认项D"：若 B7（AI顾问）使用不同标签值（如 `benign`/`uncertain`/`dangerous`），决策表全部回退 ManualReview。需建立 B3↔B7 之间的字符串契约。 | **中风险**。B7 尚未实现，标签命名偏差是实际可能发生的集成问题。 | **方案**：创建 `AiLabelConstants` 常量类，集中定义三个标签值。B7 开发者直接引用常量，编译期消除拼写错误。决策引擎代码中所有硬编码字符串替换为常量引用。 | ```diff
- if (aiLabel.Equals("safe", StringComparison.OrdinalIgnoreCase))
+ if (aiLabel.Equals(AiLabelConstants.Safe, StringComparison.OrdinalIgnoreCase))
```
|
| 6 | 待确认C：`IProgress<int>` vs `IProgress<OperationProgress>` | 对照 `OperationProgress` DTO（`Core/DTOs/OperationProgress.cs`）和 `MainViewModel.cs:399-414` 的适配器代码。UI 层已完成 `IProgress<int>` → `OperationProgress` 的适配转换。 | 无需修改。当前架构正确：B3 使用简洁的 `IProgress<int>`，UI 层负责适配为富进度 DTO。`OperationProgress` 的 `Phase.Hashing` 和 `CurrentFilePath` 字段已定义，供 B4 使用。 | **确认保持现状**。理由：(1) `Parallel.ForEachAsync` 回调中每秒触发数十次进度上报，`CurrentFilePath` 在并行场景下跳动过快，实用价值有限；(2) 适配逻辑集中在 UI 层符合架构分层。 | 无代码变更。 |

## 3. 验证与测试

- [x] 编译通过：`dotnet build — 0 错误`
- [x] 核心单元测试通过：`DecisionEngineTests` 53/53 通过（含决策表4种情况、AI标签大小写、空输入、并发安全、取消、文件不存在、SHA-256正确性、进度上报、不可变性、JSON序列化）
- [x] 全量回归测试：`786 通过 / 9 跳过 / 55 失败`（55 个失败均为预存在问题：Everything SDK 不可用 15 个、数据库/文件系统集成测试 25 个、UI 测试 12 个、其他 3 个——均与本次修改无关）

## 4. 回滚与运维指南

- **回滚操作**：`git revert 1201eda3ddd949f84937ffc4580159fdc1ae2f6a`
- **依赖变更**：无。新增 `AiLabelConstants` 为纯常量类，无外部依赖。
- **配置迁移**：`UserConfig.HashConcurrency` 默认值 0 保持向后兼容（行为不变）。已有配置文件无需迁移。
- **注意事项**：
  - `AiLabelConstants` 定义了 B3↔B7 的字符串契约。B7 实现时必须引用此常量类，否则存在标签不匹配风险。
  - `HashConcurrency` 为可选优化项，不设置（=0）时行为与修改前完全一致。

## 5. 预防措施与后续 Action

- **新增静态分析规则**：建议在 B7 开发时添加架构测试，验证 AI 标签值来自 `AiLabelConstants`（可用 Roslyn Analyzer 或简单 grep 检查）。
- **文档更新**：
  - `docs/任务实现细节/B3-决策引擎（仲裁逻辑+快照生成）.md` 中的待确认项 A/D 状态需更新为"已解决"。
  - B7 任务文档应引用 `AiLabelConstants` 作为标签值规范。
- **后续跟踪**：
  - 待确认项 B（失败文件列表返回 UI）：`SkippedEntries` 已实现，UI 层可随时接入展示。
  - 待确认项 C（Progress 类型）：确认保持 `IProgress<int>` 不变，无需后续动作。
