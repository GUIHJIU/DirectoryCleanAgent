# 代码审计修复报告 — C9 非管理员只读模式处理

- **审计日期**: 2026-07-09
- **设计基线**: V3.7

---

## 1. 修复概述

- **修复分支**: `master`
- **Commit Hash**: `fc8eb21`
- **涉及文件数**: 8 个

| 文件 | 变更类型 | 说明 |
|------|----------|------|
| `src/DirectoryCleanAgent/Views/QuarantineWindow.xaml` | 修复 | B1: 布局叠加 → StackPanel 垂直排列 |
| `src/DirectoryCleanAgent.Core/Localization/Strings.resx` | 修复 | Q1: 更新 `Status.ReadOnlyMode` / `Warning.IndexingIncomplete` 完整文本 |
| `src/DirectoryCleanAgent.Core/Localization/Strings.en.resx` | 修复 | Q1: 同上，英文翻译 |
| `src/DirectoryCleanAgent/ViewModels/MainViewModel.cs` | 修复 | Q1: `UpdateWarningBanner()` 改用 `_localization.GetString()` |
| `src/DirectoryCleanAgent/ViewModels/QuarantineViewModel.cs` | 修复 | Q1: `ReadOnlyWarningMessage` 改用 `_localization.GetString()` |
| `src/DirectoryCleanAgent.Tests/UI/MainViewModelTests.cs` | 修复 | Q1: Mock `ILocalizationService.GetString` 返回值 |
| `src/DirectoryCleanAgent.Tests/UI/QuarantineViewModelTests.cs` | 修复 | Q1: Mock `ILocalizationService.GetString` 返回值 |
| `docs/任务实现细节/C9-非管理员只读模式处理.md` | 更新 | Q2: 待确认项 B 标记为已验证非缺陷，补充 B1 修复记录 |

---

## 2. 问题详情与修复

| 序号 | 审查路径 | 如何发现问题 | 影响范围（风险等级） | 思考修复方案（权衡） | 修复方案解析（代码对比） |
| :--- | :--- | :--- | :--- | :--- | :--- |
| 1 | `QuarantineWindow.xaml:36-120` — 内层 Grid.Row="0" 中容量信息栏、错误提示、只读警告三个 Border 同 cell 叠加 | 对照实现细节文档§一-决策3："QuarantineWindow.xaml 添加内联只读警告 Border"。审查 XAML 结构时发现三个 Border 均声明 `Grid.Row="0"` 而无垂直排列容器，WPF Grid 同 cell 元素按文档顺序叠加渲染，非管理员打开隔离区窗口时只读警告覆盖在容量信息栏上方 | **中** — 非管理员用户打开隔离区窗口时 UI 错乱（两个 Border 叠加），功能正常但用户体验差 | **方案A（采纳）**: 用 `StackPanel` 包裹三个 Border，最小改动，无逻辑影响。**方案B（否决）**: 将内层 Grid 扩展为 4 行，每个 Border 独占一行。否决原因：改动量大，Grid.Row 索引需全部重新编排，且三个 Border 的显示/隐藏由 Visibility 绑定控制，独占行会产生空白间距 | **修改前**: `<Border Grid.Row="0" ...>容量信息栏</Border>` `<Border Grid.Row="0" ...>错误提示</Border>` `<Border Grid.Row="0" ...>只读警告</Border>` — 三个 Border 平铺在同一 Grid cell，叠加渲染。**修改后**: `<StackPanel Grid.Row="0">` `<Border ...>容量信息栏</Border>` `<Border ...>错误提示</Border>` `<Border ...>只读警告</Border>` `</StackPanel>` — StackPanel 垂直排列，每个 Border 独立占一行 |
| 2 | `MainViewModel.cs:1341` + `QuarantineViewModel.cs:231` — 两处硬编码中文字符串 `"⚠️ 只读模式 — ..."` 未使用本地化资源 | 对照实现细节文档§四-待确认项A：文档明确标注两处硬编码字符串应统一迁移到 `Strings.resx`。审查时发现本地化基础设施（A6）已就绪，`ILocalizationService` 已注入两个 ViewModel，但字符串仍为硬编码 | **低** — 功能正确但违反项目本地化规范，且英文用户无法看到翻译。项目中其他 50+ 个 UI 字符串均已通过资源文件本地化 | **方案A（采纳）**: 利用已有资源键 `Status.ReadOnlyMode` 和 `Warning.IndexingIncomplete`，更新其值为完整文本，代码改为 `_localization.GetString(...)`。资源键已存在仅需更新值，改动最小。**方案B（否决）**: 新建资源键。否决原因：键已存在，新增键会造成冗余 | **修改前** (`MainViewModel.cs`): `WarningMessage = "⚠️ 只读模式 — 请以管理员身份重新运行以启用清理功能（非管理员模式下数据分析功能仍可正常使用）";` **修改后**: `WarningMessage = _localization.GetString("Status.ReadOnlyMode");` **修改前** (`QuarantineViewModel.cs`): `public string ReadOnlyWarningMessage => "⚠️ 只读模式 — ...";` **修改后**: `public string ReadOnlyWarningMessage => _localization.GetString("Status.ReadOnlyMode");` 资源文件同步更新：`Strings.resx` 中 `Status.ReadOnlyMode` 的值从简版更新为完整版（含⚠️和括号注释），`Strings.en.resx` 新增对应英文翻译 |
| 3 | `AppStateService.StateChanged` 事件订阅者追踪 — 测试中发现 += 调用 2 次 | 对照实现细节文档§四-待确认项B：文档标注 `MainViewModelTests` 中 `StateChanged +=` 调用 2 次为预存缺陷待排查。通过 `grep` 全项目搜索 `StateChanged +=` 订阅点，追踪调用链 | **无** — 经代码追踪验证非缺陷 | 无需修复。根因分析：`StateChanged` 有两个独立合法订阅者——`MainViewModel.OnAppStateChanged`（MainViewModel.cs:102）和 `FileListViewModel.OnAppStateChanged`（FileListViewModel.cs:323），各自处理不同 UI 关注点（命令状态刷新 vs 文件列表加载协调）。这是标准 MVVM 多 ViewModel 观察者模式。测试已正确使用 `Times.AtLeastOnce` 覆盖。仅更新实现细节文档将状态从"待确认"改为"已验证非缺陷" | 无代码变更。**文档更新**: `docs/任务实现细节/C9-非管理员只读模式处理.md` §四-待确认项B 从 `该问题为预存缺陷...需在后续任务中排查` 更新为 `✅ 已验证非缺陷 — StateChanged 有两个合法的独立订阅者...这是标准的观察者模式，非重复订阅缺陷` |

---

## 3. 验证与测试

- [x] **编译通过**: `dotnet build` — 0 错误（99 个预存 xUnit 分析器警告，非本次引入）
- [x] **单元测试通过**: `dotnet test --filter "FullyQualifiedName~NonAdminMode|FullyQualifiedName~QuarantineViewModel|FullyQualifiedName~MainViewModelTests"` — **19/19 通过**，0 失败，0 跳过
- [x] **C9 核心场景覆盖**:
  - `NonAdminMode_AppMode_ReadOnly_WhenNotAdmin` ✅
  - `NonAdminMode_DeleteOperation_Disabled` ✅
  - `NonAdminMode_WarningBanner_Displayed` ✅
  - `Initialize_SyncsIsAdminFromAppStateService` ✅
  - `Initialize_NonAdmin_ShowsReadOnlyWarning` ✅
  - `IsAdmin_False_DisablesCleanCommands` ✅
  - `IsIndexReady_False_DisablesCleanAndShowsWarning` ✅
  - `AdminAndIndexReadyAndReadyState_EnablesCleanCommands` ✅
  - `IsAdmin_ReflectsAppStateService` ✅
  - `NonAdmin_ShowsReadOnlyWarning_DisablesCommands` ✅
  - `AppModeChanged_ToReadOnly_RefreshesCommandStates` ✅
  - `AppModeChanged_ToNormal_RemovesReadOnlyWarning` ✅
  - `Constructor_SubscribesToAppModeChanged` ✅

---

## 4. 回滚与运维指南

- **回滚操作**: `git revert fc8eb21`
- **依赖变更**: 无
- **数据库 Schema 变更**: 无
- **配置变更**: 无
- **注意事项**: 资源文件 `Strings.resx`/`Strings.en.resx` 的 `Status.ReadOnlyMode` 和 `Warning.IndexingIncomplete` 键值已更新为完整文本（含 emoji 前缀）。若其他模块直接引用了这些键的旧值，回滚后需同步检查。

---

## 5. 预防措施与后续 Action

- **静态扫描规则**: 建议引入 Roslyn 分析器规则 `CA1303: Do not pass literals as localized parameters`，在编译时拦截硬编码用户可见字符串
- **文档已更新**: `docs/任务实现细节/C9-非管理员只读模式处理.md` — 待确认项 A/B 均已更新为最终状态，补充 B1 修复记录
- **后续关注**: `MainViewModel.UpdateWarningBanner()` 中的 WarningMessage 在语言运行时切换后不会自动刷新（缓存于字段），需在 `OnAppModeChanged` 中调用 `UpdateWarningBanner()` 才能更新。当前 AppMode 运行时不变（设计假设），若未来支持运行时语言切换需额外处理
