# 代码审计修复报告 - B6-隔离区管理 / 2026-07-05

## 1. 修复概述

- **修复分支**：`master`（直接在 master 上修复）
- **Commit Hash**：`e2327c1b941ceb661a394c160abef652f8e04cf5`
- **涉及文件数**：6个（修改6个）

| 文件 | 操作 | 说明 |
|------|------|------|
| `src/DirectoryCleanAgent.Core/Interfaces/IQuarantineManager.cs` | 修改 | 方法重命名 `CanAccommodateAsync` → `HasCapacityAsync` |
| `src/DirectoryCleanAgent.Operations/QuarantineManager.cs` | 修改 | 方法重命名 + `ListQuarantineFilesAsync` 进度报告 Phase 修正 |
| `src/DirectoryCleanAgent.Core/DTOs/OperationProgress.cs` | 修改 | 新增 `Scanning` 枚举值 |
| `src/DirectoryCleanAgent/Models/StatusBarInfo.cs` | 修改 | 新增 `Scanning` 状态栏文本分支 |
| `src/DirectoryCleanAgent.Tests/Operations/QuarantineManagerTests.cs` | 修改 | 同步重命名测试方法（6处） |
| `docs/任务分解-补充说明.md` | 修改 | 更新 4.6 节架构描述，反映最终设计决策 |

## 2. 问题详情与修复

| 序号 | 审查路径 | 如何发现问题 | 影响范围（风险等级） | 思考修复方案（权衡） | 修复方案解析（代码对比） |
| :--- | :--- | :--- | :--- | :--- | :--- |
| 1 | `IQuarantineManager.cs:48` — 接口方法 `CanAccommodateAsync` 与补充说明 4.6 节规格 `HasCapacityAsync` 命名不一致 | 对照 `docs/任务分解-补充说明.md` 4.6 节逐方法比对：规格定义 `Task<bool> HasCapacityAsync(long fileSize, CancellationToken ct)`，代码实现命名为 `CanAccommodateAsync`。全量搜索确认该方法无生产代码调用方（OperationExecutor 使用 `EnforceCapacityAsync` 做批量容量检查），仅 6 处测试引用。 | 低风险。仅命名不一致，语义完全等价（检查隔离区是否有空间容纳文件）。无生产调用方，重命名影响仅限接口+实现+测试。 | **方案A（选用）**：重命名为 `HasCapacityAsync` 以匹配规格文档。理由：(1) `Has*` 前缀是 .NET 标准布尔返回方法命名惯例；(2) 补充说明是权威规格，代码应服从规格；(3) 影响面极小，仅 0 生产调用方。**方案B（否决）**：修改规格文档以匹配代码——规格作为设计基准不应被代码反向驱动修改。 | ```diff
namespace DirectoryCleanAgent.Core.Interfaces;
/// - 容量阈值强制检查（QuarantineMaxSizeBytes）
/// - 大文件绕过判断（隔离区卷剩余空间 20% 阈值）
/// ...
-    Task<bool> CanAccommodateAsync(long fileSize, CancellationToken ct = default);
+    Task<bool> HasCapacityAsync(long fileSize, CancellationToken ct = default);
```
```diff
// QuarantineManager.cs 实现同步修改
-    public async Task<bool> CanAccommodateAsync(long fileSize, CancellationToken ct = default)
+    public async Task<bool> HasCapacityAsync(long fileSize, CancellationToken ct = default)
```
```diff
// QuarantineManagerTests.cs 测试方法同步重命名（6处）
-    // CanAccommodateAsync 测试
+    // HasCapacityAsync 测试
-    public async Task CanAccommodateAsync_WhenDisabled_ShouldReturnFalse()
+    public async Task HasCapacityAsync_WhenDisabled_ShouldReturnFalse()
-    public async Task CanAccommodateAsync_WithinLimit_ShouldReturnTrue()
+    public async Task HasCapacityAsync_WithinLimit_ShouldReturnTrue()
-        var result = await sut.CanAccommodateAsync(1024);
+        var result = await sut.HasCapacityAsync(1024);
```
 |
| 2 | `docs/任务分解-补充说明.md:270-278` — 4.6 节定义的 `StageAsync` 方法未在 `IQuarantineManager` 接口中实现，文件暂存逻辑保留在 `OperationExecutor.BackupToQuarantineAsync`（B4）中 | 对照规格 4.6 节与实际接口逐方法比对：规格定义了 `StageAsync(string sourcePath, string fileName, long fileSize, long volumeFreeSpace, bool sameVolume, IProgress<long>? progress, CancellationToken ct)`，但接口中无此方法。深入分析调用链发现：同卷移动/跨卷复制/SHA-256 校验/`.cleaning.tmp` 重命名全部在 `OperationExecutor` 的私有方法中实现。B6 实现文档一.1 节将此列为显式设计决策。 | 低风险。仅文档与代码不一致。当前架构拆分合理——QuarantineManager 负责策略与元数据层（命名/容量/过期/统计），OperationExecutor 负责执行机制层（IFileOperation/HashVerifier/文件系统操作）。若强行将 `StageAsync` 纳入 QuarantineManager 会导致其对 `ShellFileOperation`、`HashVerifier`、`DeleteSnapshotEntry` 的依赖，破坏层次隔离。 | **方案（选用）**：更新补充说明 4.6 节，移除 `StageAsync`，列出 B4 实际调用的 QuarantineManager 方法（`GetQuarantineDirectoryAsync` / `HasCapacityAsync` / `EnforceCapacityAsync` / `IsLargeFileForQuarantineAsync` / `ConstructQuarantineFileName`），并补充架构决策说明。**替代方案（否决）**：在接口中添加 `StageAsync` 并从 OperationExecutor 迁移逻辑——会导致 QuarantineManager 依赖 Operations 层组件（ShellFileOperation），形成反向依赖。 | ```diff
### 4.6 B6（隔离区管理）——与 B4 隔离区路径的交互

-B4 在执行隔离区删除时，调用 B6 提供的以下方法：
-```csharp
-// 检查隔离区是否有足够空间容纳指定大小的文件
-Task<bool> HasCapacityAsync(long fileSize, CancellationToken ct);
-
-// 移动/复制文件到隔离区（返回隔离区中的最终路径）
-Task<string> StageAsync(
-    string sourcePath,      // \\?\ 格式
-    string fileName,
-    long fileSize,
-    long volumeFreeSpace,
-    bool sameVolume,
-    IProgress<long>? progress,
-    CancellationToken ct);
-```
-
-B4 不需要知道 B6 的内部实现（跨卷复制、`.cleaning.tmp`、校验后重命名），仅通过接口调用。
+B4 在执行隔离区删除时，调用 B6 提供的以下方法：
+
+```csharp
+// 获取隔离区目录路径（自动创建，禁用时返回 null）
+Task<string?> GetQuarantineDirectoryAsync(CancellationToken ct);
+
+// 检查隔离区是否有足够空间容纳指定大小的文件
+Task<bool> HasCapacityAsync(long fileSize, CancellationToken ct);
+
+// 容量强制检查：当前已用 + 新增是否超出配额
+Task<bool> EnforceCapacityAsync(long additionalBytes, CancellationToken ct);
+
+// 判断文件是否过大而应绕过隔离区直接删除（阈值：卷剩余空间 20%）
+Task<bool> IsLargeFileForQuarantineAsync(long fileSize, CancellationToken ct);
+
+// 构造符合命名规范的隔离区文件名
+string ConstructQuarantineFileName(string sha256Hash, string originalFileName);
+```
+
+**架构决策（B6 实现阶段确定）**：文件暂存执行逻辑（同卷移动/跨卷复制/SHA-256 校验/.cleaning.tmp 重命名）保留在 B4 的 `OperationExecutor.BackupToQuarantineAsync` 中，不在 B6 接口中暴露 `StageAsync`。QuarantineManager 负责策略与元数据层（命名/容量/过期/统计），OperationExecutor 负责执行机制层（IFileOperation/HashVerifier/文件系统操作）。此拆分避免了 QuarantineManager 对 ShellFileOperation、HashVerifier 的依赖，保持层次隔离。
+
+B4 不需要知道 B6 的内部实现（过期清理、空间统计、文件命名解析），仅通过接口调用策略方法。
```
 |
| 3 | `QuarantineManager.cs:719` — `ListQuarantineFilesAsync` 进度报告使用 `OperationPhase.Hashing`，但实际执行的是文件枚举操作，非哈希计算 | 审查 `ListQuarantineFilesAsync` 方法体时发现：每个文件的进度报告 `Phase = OperationPhase.Hashing`，但该方法的功能是枚举 `.quarantine` 文件并解析文件名元数据（QuarantineFileEntry），不涉及任何 SHA-256 计算。对比现有 `OperationPhase` 枚举值（CheckingCapacity/Hashing/Deleting/Verifying/Restoring/Simulating/Completed/Cancelled），无一匹配"扫描/枚举"语义。 | 低风险。仅影响 C7 隔离区界面的进度条文本显示。`StatusBarInfo.UpdateStatusText` 的 switch 表达式带 `_ => "正在处理..."` 默认臂（StatusBarInfo.cs:55），即使不修复也不会崩溃。但用户会看到"正在计算文件哈希..."的错误提示。 | **方案A（选用）**：添加 `Scanning` 枚举值到 `OperationPhase`，修正引用点，并在 `StatusBarInfo` 中添加显式文本分支。理由：(1) 语义正确——文件枚举是独立的操作阶段；(2) 枚举添加非破坏性——所有现有 switch 都有默认臂；(3) 为未来其他枚举操作提供正确的 Phase 值。**方案B（否决）**：复用现有 `Simulating` 阶段——语义不匹配，Simulating 是 C5 模拟运行专用。 | ```diff
// OperationProgress.cs: OperationPhase 枚举
+    /// <summary>正在扫描/枚举文件（隔离区文件列表、目录遍历等）</summary>
+    Scanning,
+
     /// <summary>正在检查回收站容量（删除前预检）</summary>
     CheckingCapacity,
```
```diff
// QuarantineManager.cs:719 — ListQuarantineFilesAsync
                         progress?.Report(new OperationProgress
                         {
-                            Phase = OperationPhase.Hashing,
+                            Phase = OperationPhase.Scanning,
                             CurrentFilePath = file,
                             CompletedCount = processedCount,
                             TotalCount = -1,
                             ProcessedBytes = 0
                         });
```
```diff
// StatusBarInfo.cs: UpdateStatusText switch 表达式
+            OperationPhase.Scanning when p.TotalCount > 0 => $"正在扫描 ({p.CompletedCount}/{p.TotalCount})...",
+            OperationPhase.Scanning => "正在扫描文件...",
             OperationPhase.CheckingCapacity => "正在检查回收站容量...",
```
 |

## 3. 验证与测试

- [x] **编译通过**：`dotnet build` — 0 错误，95 警告（均为预存 NU1900/xUnit2002 警告，非本次引入）
- [x] **单元测试通过**：`dotnet test --filter "FullyQualifiedName~QuarantineManagerTests"` — 38/38 通过，0 失败，0 跳过
- [x] **重命名验证**：全量搜索 `CanAccommodateAsync` — 0 结果，确认所有引用已更新
- [x] **枚举兼容性**：全量搜索 `OperationPhase` 的 switch 使用点 — 所有 switch 均带 `_` 默认臂或非 exhaustive 模式匹配，新增枚举值无破坏性

## 4. 回滚与运维指南

- **回滚操作**：`git revert e2327c1b941ceb661a394c160abef652f8e04cf5`
- **依赖变更**：无（不涉及数据库 Schema / Redis Key / API 接口签名变更）
- **注意**：若回滚，`docs/任务分解-补充说明.md` 4.6 节将恢复到含 `StageAsync` 的旧版描述（与实际代码不一致），需手动处理

## 5. 预防措施与后续 Action

- **静态扫描规则**：本次未引入新规则。建议后续在 CI 中增加接口方法与规格文档的命名一致性检查（如自定义 Roslyn Analyzer）
- **文档更新**：`docs/任务分解-补充说明.md` 4.6 节已更新，反映 B6 最终架构
- **待确认项跟踪**：B6 文档中的 4 项待确认项（A: 10GB 默认值 / B: NTP 时钟回退 / C: DriveFreeBytes=0 UI 处理 / D: BackupManager 集成测试）均已记录，非 B6 代码缺陷，延后至对应模块处理
- **偏差4撤回记录**：审查中发现的"取消后丢失部分结果"经二次跟踪执行流确认为假阳性——外层 `catch (OperationCanceledException)` 仅在 `GetQuarantineDirectoryAsync` 抛 OCE 时触发（此时无工作完成，返回 0 正确），内层取消由内层 catch 捕获后沿正常路径返回实际计数。取消语义处理完全正确
