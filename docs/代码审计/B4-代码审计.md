# 代码审计修复报告 - B4-操作执行器 / 2026-07-04

## 1. 修复概述

- **审计模块**：B4-操作执行器（乐观删除+回收站+跨卷复制）
- **修复分支**：`master`（直接提交）
- **Commit Hash**：`ad1c5ed`
- **涉及文件数**：5 个
- **审计依据**：
  - `docs/任务实现细节/B4-操作执行器（乐观删除+回收站+跨卷复制）.md`（设计决策与假设清单）
  - `docs/任务分解.md` B4 DoD
  - `docs/任务分解-补充说明.md` 4.5 节（容量预检时序）
  - `docs/智能磁盘清理工具-总体设计文档-V3.7.md`（设计基线）

### 审计结论概览

| 分类 | 数量 |
|------|------|
| ✅ 符合 DoD | 6/6 项全部通过 |
| ⚠️ 偏差（已修复） | 7 项 |
| ❌ 缺失 | 0 项 |
| ❓ 待确认（非代码问题） | 4 项（来自设计文档的待确认项） |

---

## 2. 问题详情与修复（核心部分）

| 序号 | 审查路径 | 如何发现问题 | 影响范围（风险等级） | 思考修复方案（权衡） | 修复方案解析（代码对比） |
| :--- | :--- | :--- | :--- | :--- | :--- |
| 1 | `OperationExecutor.cs:269-273, 306-313` — 锁失败文件错误写入墓碑 | 逐行追踪 ExecuteAsync 控制流：`manual_review` 分支未 `continue`，代码落入第 329 行公共的墓碑/删除记录/审计日志写入区 | **中高**：锁失败文件未被删除但写入了墓碑，导致 B1 后续扫描跳过这些文件（命中墓碑过滤），用户永久失去处理机会。审计日志错误记录为 `"FILE_DELETED"+"SUCCESS"` | **方案A**：在 manual_review 分支加 `continue` 跳过墓碑区，单独写入降级审计日志。**方案B**：引入特殊的"待处理墓碑"类型，超时后自动失效。选择方案A——最小改动，语义清晰，墓碑仅用于已删除文件 | **修复前**：lock violation 分支无 `continue`，落入 `CreateTombstone()` + `CreateDeletionRecord()` + `AuditLogEntry("FILE_DELETED","SUCCESS")`。**修复后**：lock violation 分支写入 `AuditLogEntry("FILE_LOCKED","DOWNGRADED_TO_MANUAL_REVIEW")` 后 `continue`，跳过墓碑/删除记录写入 |
| 2 | `OperationExecutor.cs:729` — `frnAvailable` 变量读取但未使用 | 静态分析：`var frnAvailable = _configService.Current.FRN_AVAILABLE` 赋值后从未被读取或用于分叉逻辑 | **低**：死代码，不影响运行时行为。但造成维护者困惑（读代码时会以为后续有 FRN 分叉逻辑） | **方案A**：删除变量。**方案B**：保留变量并增加注释说明 FRN 信息在 B4 阶段不可用。选择方案B——保留上下文信息，注释明确说明 `DeleteSnapshotEntry` 不含 VolumeGuid/FRN 字段的原因 | **修复前**：`var frnAvailable = ...; var fingerprintKey = ...;`（变量未使用）。**修复后**：移除变量，展开注释为 4 行说明 FRN 不可用的原因 + 未来扩展方向 |
| 3 | `OperationExecutor.cs:577, 645` — 隔离区路径中 ERROR_FILE_NOT_FOUND 不一致处理 | 对比直接删除路径（line 298：`deleteHr == S_OK \|\| deleteHr == ERROR_FILE_NOT_FOUND`→成功）与隔离区备份路径（BackupToQuarantineAsync 无此检查） | **中**：文件在快照生成后被其他进程删除时，直接删除路径正确计为成功，隔离区路径错误计为失败。两个路径对"文件已不存在"的语义应一致 | 在 `BackupToQuarantineAsync` 同卷 MoveFile 检查和跨卷 CopyFile 检查中增加 `ERROR_FILE_NOT_FOUND` → `Success=true` 的判断。与直接删除路径完全一致 | **修复前**：`if (moveHr == Shell32Native.S_OK)` 仅检查成功。**修复后**：`if (moveHr == Shell32Native.S_OK \|\| moveHr == Shell32Native.ERROR_FILE_NOT_FOUND)`，跨卷复制同理 |
| 4 | `OperationExecutor.cs:761` — DecisionSnapshotJson 写入空 JSON | 审查 `CreateDeletionRecord` 方法：发现 `DecisionSnapshotJson = "{}"` 固定写死 | **低**：丢失审计追踪信息。回滚时 B5 的 `BackupManager` 依赖 DeletionRecord 中的快照数据进行哈希校验和路径解析，空 JSON 无法提供这些信息 | 使用 `System.Text.Json.JsonSerializer.Serialize(entry)` 序列化完整的 `DeleteSnapshotEntry`。包含 FilePath、FileSize、Sha256Hash、FinalAction、RuleVerdict、OperationId 全部字段 | **修复前**：`DecisionSnapshotJson = "{}"`。**修复后**：`DecisionSnapshotJson = System.Text.Json.JsonSerializer.Serialize(entry)` |
| 5 | `Shell32Native.cs:98-117` — MoveFileExW P/Invoke 声明未使用 | 全局搜索 `MoveFileExW` 引用：仅在 Shell32Native.cs 中声明，`CrossVolumeQuarantineAsync` 实际使用 `File.Move()`（.NET BCL）而非 P/Invoke | **低**：死代码。注释写明用于 `.cleaning.tmp→.quarantine` 重命名，但实际未调用 | **方案A**：移除声明，保持代码精简。**方案B**：将 `File.Move` 替换为 `MoveFileExW(..., MOVEFILE_WRITE_THROUGH)` 确保立即落盘。选择方案A——`File.Move` 在同卷时行为完全正确（原子重命名），无需引入额外 P/Invoke | **修复前**：Shell32Native.cs 包含 `MoveFileExW` 声明 + `MOVEFILE_REPLACE_EXISTING`/`MOVEFILE_WRITE_THROUGH` 常量（24行）。**修复后**：全部移除 |
| 6 | `DirectoryCleanAgent.Operations.csproj:3` — TFM 使用 net8.0 而非 net8.0-windows | 对照设计文档边界第5条：明确要求 `net8.0-windows` 限定 Windows 平台 | **低**：当前 `net8.0` 可通过手动 P/Invoke 编译通过，但与设计文档不一致，且其他项目（Tests）已使用 `net8.0-windows` | 直接修改 TFM，无权衡。所有代码依赖 Windows Shell API（SHFileOperationW、SHQueryRecycleBinW、WindowsIdentity），net8.0-windows 提供编译时平台检查 | **修复前**：`<TargetFramework>net8.0</TargetFramework>`。**修复后**：`<TargetFramework>net8.0-windows</TargetFramework>` |
| 7 | `HashVerifier.cs:65` vs `BackupManager.cs:993` — SHA-256 计算代码重复 | 交叉比对 Operations 项目中的哈希计算方法：发现 `HashVerifier.ComputeSha256Async`（private）与 `BackupManager.ComputeFileHashAsync`（private）实现完全一致（4KB 缓冲区、FileShare.Read、小写十六进制、相同异常处理） | **低**：两处独立维护的相同逻辑，未来若修改哈希策略（如切换 BLAKE3）容易遗漏一处 | 将 `HashVerifier.ComputeSha256Async` 从 `private` 改为 `public static`，`BackupManager.ComputeFileHashAsync` 改为委托调用 `HashVerifier.ComputeSha256Async`。统一哈希入口 | **修复前**：两个类中各有 ~25 行完全相同的 SHA-256 流式计算代码。**修复后**：`BackupManager.ComputeFileHashAsync` 缩减为单行委托 `=> HashVerifier.ComputeSha256Async(filePath, ct)` |

---

## 3. 验证与测试

- [x] **编译通过**：`dotnet build` — 0 错误，88 警告（均为项目已有警告，非本次引入）
- [x] **单元测试通过**：`dotnet test --filter "FullyQualifiedName~OperationExecutor"` — 19/19 通过
- [x] **回归验证**：所有 5 个变更文件编译无错误
- [ ] **手动验证（待执行）**：
  - 锁失败降级流程：模拟文件被占用的删除场景，确认不再写入墓碑、审计日志正确记录 `FILE_LOCKED`
  - 隔离区 FILE_NOT_FOUND：在快照生成后手动删除文件，确认隔离区路径正确计为成功
  - DecisionSnapshotJson 序列化：确认 JSON 输出包含完整字段

### 测试覆盖缺口（建议后续补充）

| 缺口 | 优先级 |
|------|--------|
| 锁失败降级 manual_review 的单元测试 | 高 |
| 隔离区备份路径（QuarantineMaxSizeBytes > 0）的测试 | 中 |
| 跨卷隔离区流程（CrossVolumeQuarantineAsync）的测试 | 中 |
| ERROR_FILE_NOT_FOUND 视为成功的测试 | 低 |

---

## 4. 回滚与运维指南

- **回滚操作**：`git revert ad1c5ed`
- **依赖变更**：无（未涉及数据库 Schema 变更、Redis Key 结构变更、NuGet 包版本变更）
- **配置变更**：无
- **注意事项**：
  1. TFM 从 `net8.0` 改为 `net8.0-windows` 后，CI 构建环境需确保安装了 .NET 8 Windows SDK（而非仅跨平台 SDK）
  2. `HashVerifier.ComputeSha256Async` 从 private 改为 public，外部项目若已引用 Operations 模块需注意 API 可见性变更

---

## 5. 预防措施与后续 Action

### 已引入的改进
- 统一了"文件不存在=删除成功"的错误处理策略（直接删除 & 隔离区两路径一致）
- 统一了 SHA-256 哈希计算入口（HashVerifier 作为唯一实现）
- 消除了死代码（MoveFileExW、frnAvailable）

### 建议后续 Action
1. **B3 协作**：在 `DeleteSnapshotEntry` DTO 中增加 `VolumeGuid`、`FileReferenceNumber`、`LastWriteTime` 字段，使 B4 墓碑可使用 FRN 精确匹配（关联 [待确认项B]）
2. **B5 审计**：复查 `BackupManager` 是否正确使用了 `DecisionSnapshotJson` 中的快照数据
3. **测试补充**：为 B4 新增 5 项测试覆盖缺口（见第 3 节）
4. **文档更新**：设计文档中 [待确认项A]（SHQUERYRBINFO 字段语义）需在实体机上验证后更新

### 设计文档待确认项（非代码问题，需人工介入）

| 编号 | 内容 | 建议 |
|------|------|------|
| A | `SHQUERYRBINFO.i64NumItems` 字段语义模糊（对象数 vs 字节数） | Windows 10/11 实体机调试验证，确认 `AvailableBytes` 计算公式正确性 |
| B | 墓碑 `OriginalLastWriteTime` 使用 `DateTime.UtcNow` 替代 | 需在 `DeleteSnapshotEntry` DTO 中增加 `LastWriteTime` 字段（影响 B3） |
| C | 隔离区目录不可独立配置 | 评估是否需在 `UserConfig` 中增加 `QuarantinePath` 字段 |
| D | 永久删除使用 SHFileOperationW vs File.Delete | 性能 profiling 后决定是否切换 |

---

**审计人**：Claude Code (Code Audit)
**审计日期**：2026-07-04
**修复日期**：2026-07-04
