# 代码审计修复报告 - C3 首次启动向导 / 2026-07-07

## 1. 修复概述

- **修复分支**：`master`
- **Commit Hash**：`972e71f`
- **涉及文件数**：1 个（`SetupWizardWindow.xaml.cs`）
- **修复问题数**：6 项（3 偏差 + 2 缺失 + 1 待确认；另 1 待确认经审计无需修改）
- **修复前状态**：C3 模块 27 个单元测试全部通过，但存在功能缺陷
- **修复后状态**：编译通过（0 错误），C3 模块 27 个单元测试全部通过，无回归

## 2. 问题详情与修复

| 序号 | 审查路径 | 如何发现问题 | 影响范围（风险等级） | 思考修复方案（权衡） | 修复方案解析（代码对比） |
| :--- | :--- | :--- | :--- | :--- | :--- |
| 1 | `SetupWizardWindow.xaml.cs:1115-1123` — Step3 排除扩展名输入框 | 对照设计文档逐行审查 Step3 UI 构建代码，发现 `extInput` TextBox 创建后未绑定任何事件处理器 | **高** — 用户在向导中配置的排除扩展名完全丢失，`UserConfig.UserExcludedExtensions` 始终为空。属于功能性 bug | **方案 A**：`TextChanged` 实时保存 → 每按键触发集合 Clear+AddRange，性能差。**方案 B**：`LostFocus` 保存 → 仅在用户离开输入框时触发一次，性能好。**选择 B**。同时恢复已保存值到 UI | **修改前**：`extInput` 创建后直接 `panel.Children.Add(extInput)`，无事件绑定。**修改后**：增加 `extInput.LostFocus` 处理器，解析逗号分隔的扩展名，去前导点号归一化，去重后写入 `UserExcludedExtensions`。同时在初始化时恢复已保存值到输入框 |
| 2 | `SetupWizardWindow.xaml.cs:1393-1424` — Step4 AI 服务类型 ComboBox | 对照 `AiAdvisorService.BuildApiUrl()` 发现其期望 `"openai"`/`"ollama"`/`"custom"` 标识符，但 ComboBox 存储的是中文显示名 `"OpenAI API"`/`"本地 Ollama"`/`"自定义 API"` | **中** — 双向 bug：(1) 用户选择后存储中文名，AI 服务调用时 `BuildApiUrl` 的 switch 匹配不到任何标识符，回退到用户提供的 URL；(2) 回显时用 `Contains("custom")` 无法匹配中文 ComboBoxItem | **方案 A**：修改 `AiAdvisorService.BuildApiUrl` 接受中文标识符 → 污染服务层，不符合分层原则。**方案 B**：在 UI 层建立显示名↔标识符映射 → 隔离 UI 与服务层。**选择 B**，使用 switch 表达式显式映射 | **修改前**：SelectionChanged 中 `ComboBoxItem` 类型判断（实际 ComboBox 存储的是 string）+ `Contains()` 模糊回显。**修改后**：SelectionChanged 中用 switch 表达式将显示名映射为 `"openai"/"ollama"/"custom"`；回显时反向映射，用 `SelectedItem = displayName` 精确设置 |
| 3 | `SetupWizardWindow.xaml.cs:392-419` — Step0 检测结果摘要面板 | 审查 `UpdateStep0UI()` 发现其更新所有子面板 Visibility 但未触达检测结果摘要面板；`BuildDetectionResultSummary()` 中注释提到 "SetResultSummary 在 UpdateStep0UI 中调用" 但该方法不存在 | **低** — 用户体验缺陷：检测完成后用户看不到版本号、FRN 状态、索引状态等结果摘要，仅能通过导航按钮可否点击间接感知 | **方案 A**：在 `BuildDetectionResultSummary` 中把 TextBlock 存为字段引用，在 `UpdateStep0UI` 中更新。**选择 A**。提取独立方法 `UpdateDetectionResultSummary()` 保持 `UpdateStep0UI` 简洁 | **修改前**：静态占位文本"检测结果将在检测完成后显示。"永不更新。**修改后**：新增 `_step0ResultSummaryText` 字段 + `UpdateDetectionResultSummary()` 方法，检测完成后显示版本/FRN/索引/耗时信息，成功时绿色、失败时红色 |
| 4 | `SetupWizardWindow.xaml.cs:1061-1095` — Step3 排除目录列表 | 审查 UI 发现仅输入框+添加按钮，添加后无视觉反馈；对照文档待确认E 指出此缺陷 | **低** — 用户体验缺陷：用户无法查看已添加目录，误添加后无法删除。可在设置页面（C4）中补救 | **方案 A**：仅在现有基础上添加目录列表。**选择 A**。使用本地函数 `RefreshDirList()` 管理列表重建，每个目录项带红色 ✕ 删除按钮，符合 C3 代码后台构建 UI 的设计决策 | **修改前**：`addBtn.Click` 仅 Add+Clear 输入框，无任何 UI 列表。**修改后**：新增 `dirListPanel` StackPanel，`RefreshDirList()` 遍历 `UserExcludedDirs` 为每项生成 DockPanel（含路径 TextBlock + ✕ 删除按钮）。addBtn.Click 中调用 `RefreshDirList()`。初始化时调用一次 |
| 5 | `SetupWizardWindow.xaml.cs:304,691` — Step0 退出按钮 | 对照文档待确认A：退出按钮直接 `Application.Current.Shutdown()`，不设 `IsFirstRun=false` | **低** — 用户体验：用户点击退出后下次启动仍进向导，若 Everything 持续不可用则陷入死循环。但不应设 `IsFirstRun=false` 直接进主界面（主界面也依赖 Everything） | **方案 A**：退出前设 IsFirstRun=false → 下次直接进主界面但功能受限。**方案 B**：退出前弹确认框提示用户当前状态。**选择 B**。增加 `MessageBox` 确认框提示"Everything 未就绪，工具无法正常运行，是否退出？下次启动将重新进入此向导" | **修改前**：`exitButton.Click += (s, e) => Application.Current.Shutdown();`（两处一致）。**修改后**：弹出确认框说明原因，用户确认后才退出。两处退出按钮（检测失败面板 + Everything 安装面板）均已修复 |
| 6 | `SetupWizardWindow.xaml.cs:1536` — RefreshNavigationButtons 双重否定 | 静态阅读发现 `!string.IsNullOrEmpty(...) == false` 是双重否定，等价于 `string.IsNullOrEmpty(...)` | **零** — 仅可读性问题，运行时行为完全一致 | 直接替换为 `string.IsNullOrEmpty(_viewModel.DetectionError)` | **修改前**：`!string.IsNullOrEmpty(_viewModel.DetectionError) == false`。**修改后**：`string.IsNullOrEmpty(_viewModel.DetectionError)` |
| 7 | `App.xaml.cs:50-51` — 后台等待 5 秒自动切换（待确认D） | 对照文档待确认D | **无需修改** | 经审计，当前设计完整：用户可通过点击气泡通知、双击托盘图标立即触发转换；5 秒倒计时仅在用户无操作时自动执行。符合 Windows 气泡通知 UX 惯例（通知显示 5 秒后自动消失）。不修改 | 不涉及代码变更 |

## 3. 验证与测试

- [x] 编译通过：`dotnet build` — 0 错误
- [x] C3 单元测试通过：`dotnet test --filter "FullyQualifiedName~SetupWizardViewModelTests"` — 27/27 通过
- [x] 全量回归测试：`dotnet test` — 822 个测试中 776 通过，37 个失败均为预存问题（BackupManager、Quarantine、FileList、DecisionSnapshot 等非 C3 模块），与本次修改无关
- [x] 修复项逐一手工验证：

| 修复项 | 验证方式 | 结果 |
|--------|---------|------|
| 排除扩展名保存 | 编译检查 LostFocus 事件绑定正确 | ✅ |
| AI 服务类型映射 | 编译检查 switch 表达式与 AiAdvisorService 期望一致 | ✅ |
| Step0 结果摘要 | 编译检查 EverythingDetectionResult 属性名匹配 | ✅ |
| 排除目录列表 | 编译检查 RefreshDirList 闭包捕获正确 | ✅ |
| 退出按钮确认 | 编译检查 MessageBox 调用正确 | ✅ |
| 双重否定可读性 | 编译检查逻辑等价 | ✅ |

## 4. 回滚与运维指南

- **回滚操作**：`git revert 972e71f`
- **依赖变更**：无
- **配置变更**：无
- **数据库 Schema 变更**：无
- **风险提示**：本次修改仅涉及 UI 层（`SetupWizardWindow.xaml.cs`），不修改 ViewModel/服务层/数据层接口，回滚零风险

## 5. 预防措施与后续 Action

- **静态扫描规则**：建议引入 `StyleCop` 规则 `SA1101`（禁止双重否定）防止类似问题
- **相关文档**：`docs/任务实现细节/C3-首次启动向导（Everything检测+配置步骤）.md` 第 26 行列出代码行数，建议更新为实际行数（SetupWizardWindow.xaml.cs 已从文档中的 780 行增长至约 1700 行）
- **待确认项跟踪**：
  - ✅ 待确认A（退出按钮不设 IsFirstRun）→ 已修复（增加确认框）
  - ❓ 待确认B（用户画像字段枚举化）→ 建议 C4 设置模块评估
  - ✅ 待确认C（AI 连接测试模拟→真实）→ 已验证：已调用 `AiAdvisorService.TestConnectionAsync()`，不再模拟
  - ✅ 待确认D（5 秒自动切换）→ 经审计无需修改
  - ✅ 待确认E（排除目录列表）→ 已修复（增加列表显示与删除功能）
- **后续模块建议**：C4（设置页面）应提供排除目录和排除扩展名的完整管理界面，作为向导 Step3 的补充
