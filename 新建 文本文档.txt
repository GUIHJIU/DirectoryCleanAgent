以下是自包含的提示词模板，/clear 后粘贴即可接续审查。每个模块一个提示词，按顺序使用：

  ---
  模板（替换 {模块ID} 和 {模块名称} 即可）

  继续方案A的逐模块审查。当前审查:  A1.5。

  ## 固定规则
  1. 先读 docs/任务实现细节/{模块ID}-*.md 提取检查清单（DoD/假设/边界/待确认项）
  2. 若无独立实现细节文档，参考 docs/任务分解.md 的 DoD + docs/任务分解-补充说明.md 的对应章节
  3. 读取对应代码，逐项对照
  4. 输出格式: ✅符合 / ⚠️偏差 / ❌缺失 / ❓待确认，每个问题附文件路径+行号+修复建议
  5. 发现的问题先展示、确认后再修复
  6. 每个模块结束时询问"继续下一模块？"

  ## 上下文
  - 项目: 智能磁盘清理工具，C# .NET 8 WPF，4层架构
  - 设计基线: V3.7 (docs/智能磁盘清理工具-总体设计文档-V3.7.md)
  - 源码根: src/
  - 上一模块: A1已审查通过

  ---
  具体模块提示词（按审查顺序）

  A1.5 — 核心服务契约：
  继续方案A的逐模块审查。当前审查: A1.5 - 核心服务契约（接口与DTO定义）。

  固定规则:
  1. 参考 docs/任务分解.md A1.5 DoD + docs/任务分解-补充说明.md 第三节（接口与DTO规范补充）
  2. 对照检查 A1.5-Mock数据样例.json 中的字段是否与代码中的 DTO 一致
  3. 读取 Core/Interfaces/ 和 Core/DTOs/ 下的代码，逐项对照
  4. 输出: ✅符合 / ⚠️偏差 / ❌缺失 / ❓待确认，附文件路径+行号+修复建议
  5. 发现问题先展示、确认后再修
  6. 结束时询问"继续A2？"

  项目: 智能磁盘清理工具 C# .NET 8 WPF | 设计基线 V3.7 | 源码根 src/ | 上一模块 A1 已通过

  A2 — Everything SDK 集成：
  继续方案A的逐模块审查。当前审查: A2 - Everything SDK集成与依赖检测。

  固定规则:
  1. 参考 docs/任务分解.md A2 DoD + docs/任务分解-补充说明.md 4.1节（FRN探测细节）
  2. 读取 src/DirectoryCleanAgent.Everything/ 下代码
  3. 特别关注: P/Invoke签名是否正确（已知历史bug）、FRN检测实现、120秒索引超时处理
  4. 输出: ✅符合 / ⚠️偏差 / ❌缺失 / ❓待确认，附文件路径+行号+修复建议
  5. 发现问题先展示、确认后再修
  6. 结束时询问"继续A3？"

  项目: 智能磁盘清理工具 C# .NET 8 WPF | 设计基线 V3.7 | 上一模块 A1.5 已通过

  A3 — SQLite 与批量写入队列（有独立实现细节文档）：
  继续方案A的逐模块审查。当前审查: A3 - SQLite数据库初始化与批量写入队列。

  固定规则:
  1. 先读 docs/任务实现细节/A3-SQLite数据库初始化与批量写入队列.md
  2. 提取检查清单（DoD/假设/边界/待确认项）
  3. 读取 src/DirectoryCleanAgent.Data/ 下对应代码，逐项对照
  4. 特别关注: WAL模式、ConcurrentQueue 500ms/200条批处理、WAL journal清理
  5. 输出: ✅符合 / ⚠️偏差 / ❌缺失 / ❓待确认，附文件路径+行号+修复建议
  6. 发现问题先展示、确认后再修
  7. 结束时询问"继续A4？"

  项目: 智能磁盘清理工具 C# .NET 8 WPF | 设计基线 V3.7 | 上一模块 A2 已通过

  A4 — 配置管理（有独立实现细节文档）：
  继续方案A的逐模块审查。当前审查: A4 - 配置管理（JSON+热加载基础设施）。

  固定规则:
  1. 先读 docs/任务实现细节/A4-配置管理（JSON+热加载基础设施）.md
  2. 提取检查清单（DoD/假设/边界/待确认项）
  3. 读取 src/DirectoryCleanAgent.Core/Config/ 下代码
  4. 特别关注: FileSystemWatcher 500ms防抖、RuleCacheVersion递增、RulesDirectoryWatcher监控路径
  5. 输出: ✅符合 / ⚠️偏差 / ❌缺失 / ❓待确认，附文件路径+行号+修复建议
  6. 发现问题先展示、确认后再修
  7. 结束时询问"继续A5？"

  项目: 智能磁盘清理工具 C# .NET 8 WPF | 设计基线 V3.7 | 上一模块 A3 已通过

  A5 — 路径标准化（有独立实现细节文档）：
  继续方案A的逐模块审查。当前审查: A5 - 路径标准化工具与审计日志基础。

  固定规则:
  1. 先读 docs/任务实现细节/A5-路径标准化工具与审计日志基础.md
  2. 提取检查清单（DoD/假设/边界/待确认项）
  3. 读取 Core/PathHandling/ 和 Data/AuditLog* 代码
  4. 特别关注: \\?\ 前缀、审计日志仅Insert无Update/Delete
  5. 输出: ✅符合 / ⚠️偏差 / ❌缺失 / ❓待确认，附文件路径+行号+修复建议
  6. 发现问题先展示、确认后再修
  7. 结束时询问"继续A6？"

  项目: 智能磁盘清理工具 C# .NET 8 WPF | 设计基线 V3.7 | 上一模块 A4 已通过

  A6 — 本地化（有独立实现细节文档）：
  继续方案A的逐模块审查。当前审查: A6 - 本地化基础设施。

  固定规则:
  1. 先读 docs/任务实现细节/A6-本地化基础设施.md
  2. 提取检查清单
  3. 读取 Core/Localization/ 代码 + Strings.resx / Strings.en.resx
  4. 输出: ✅符合 / ⚠️偏差 / ❌缺失 / ❓待确认
  5. 发现问题先展示、确认后再修
  6. 结束时询问"继续B1？"

  项目: 智能磁盘清理工具 C# .NET 8 WPF | 设计基线 V3.7 | 上一模块 A5 已通过

  ---
  B1~B7、C1~C10 的格式同上，只需替换模块ID和文档路径。提示词的核心结构已经稳定，后续模块只需改 3
  个地方：模块ID、文档路径、特别关注点。